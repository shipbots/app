# Expo HAS CHANGED

This app is pinned to **Expo SDK 56** (so it runs in the current Play Store
Expo Go). Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/
before writing any code. Do not bump the SDK without a matching Expo Go / dev build.
