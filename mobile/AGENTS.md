# Expo HAS CHANGED

This app is pinned to **Expo SDK 54** (the SDK this team's Play Store Expo Go
runs — 54.0.x). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/
before writing any code. Do not bump the SDK without a matching Expo Go / dev build.
Distribution is a **preview APK via EAS** (`eas build -p android --profile preview`).
